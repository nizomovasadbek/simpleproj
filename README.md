# Simple OS that made on the way to learning which written in C!
to make a floppy disk image
```bash
make
```
to Run:
```bash
make run -s
```

Required packages:
+ The GNU Linker (i386)
+ NASM
+ C compiler (i386)
+ QEMU
